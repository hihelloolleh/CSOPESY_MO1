# config.txt
num-cpu 4
scheduler "rr"
quantum-cycles 5000
batch-process-freq 1

# --- THE IMPORTANT CHANGES ARE HERE ---
min-ins 8000         # Give processes a lot more work to do
max-ins 10000        # A much larger range of instructions
delay-per-exec 10   # The MOST IMPORTANT CHANGE: Slows down the CPU
                    # This adds a 10 millisecond delay after each instruction.

max-overall-mem 16384
mem-per-frame 256
min-mem-per-proc 1024
max-mem-per-proc 4096